{
  "name": "lsi-contrats",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "pnpm -r test",
    "test:isolation": "pnpm --filter @lsi/persistence test:isolation",
    "lint": "eslint .",
    "typecheck": "pnpm -r exec tsc --noEmit"
  },
  "devDependencies": {
    "eslint": "^9.15.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0"
  }
}
